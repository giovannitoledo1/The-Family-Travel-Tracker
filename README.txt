Family Travel Tracker:
is a full-stack web application that allows users to track the countries they’ve visited and visually highlight them on an interactive world map. 
Each family member is assigned a unique color, and the application allows switching between users to view and update their travel history. 
Users can also add new family members and record their travel activities.

Features:

User Management: Add, view, and switch between different family members.

Country Tracking: Track countries each family member has visited.

Interactive Map: Countries visited by each family member are highlighted in their assigned color.

Dynamic Routing: Backend routes to handle adding countries, switching users, and creating new users.

PostgreSQL Database: Used for storing users and their visited countries.

Technology Stack:

Backend: Node.js, Express.js

Frontend: EJS (Embedded JavaScript), JavaScript, HTML, CSS

Database: PostgreSQL